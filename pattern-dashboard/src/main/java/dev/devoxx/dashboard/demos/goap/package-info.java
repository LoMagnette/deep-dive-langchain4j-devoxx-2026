/**
 * <b>GOAP</b>
 *
 * <p>A precondition chain nobody has to be told about: you cannot practise recall at the park
 * before it works in the garden, and it will not work in the garden before it works indoors.
 * Three agents whose I/O keys only fit together one way — which is why the planner has something
 * to discover. They are deliberately registered in the WRONG order in the wiring.
 */
package dev.devoxx.dashboard.demos.goap;
