/**
 * <b>Voting / ensemble</b>
 *
 * <p>Three voters with DELIBERATELY DIFFERENT criteria over the same household, so they can
 * genuinely split — which is the only way a majority means anything. Three copies of one prompt
 * always agree, and then the tally is decoration. Each answers in one word, because that is what
 * a voting strategy can tally: a one-line reason per voter would make every answer unique and no
 * majority could ever form. The reasoning is still visible — the result pane shows all three
 * votes side by side.
 */
package dev.devoxx.dashboard.demos.voting;
