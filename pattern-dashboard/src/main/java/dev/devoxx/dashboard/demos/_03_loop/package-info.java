/**
 * <b>Loop / iterative refinement</b>
 *
 * <p>The critic scores against FOUR RULES THE ROOM AGREES WITH ON SIGHT. That is the whole point: a
 * critic scoring "quality" out of 1.0 gives a number nobody watching can check, so the loop
 * becomes a light show. Here the audience can hold the draft against the same four rules and see
 * which one each pass fixes.
 */
package dev.devoxx.dashboard.demos._03_loop;
