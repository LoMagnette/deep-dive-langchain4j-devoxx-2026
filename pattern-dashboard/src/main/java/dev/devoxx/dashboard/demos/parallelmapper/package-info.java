/**
 * <b>Parallel mapper</b>
 *
 * <p>The best kind of scatter/gather demo: the room already knows every answer. Grapes and
 * chocolate are dangerous, cheddar and bread are not. The width of the fan-out is data, and the
 * cost caveat needs no explaining either — a shopping list is fifty calls.
 */
package dev.devoxx.dashboard.demos.parallelmapper;
