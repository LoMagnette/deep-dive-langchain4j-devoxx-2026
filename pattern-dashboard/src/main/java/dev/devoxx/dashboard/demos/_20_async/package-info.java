/**
 * <b>Asynchronous agents</b>
 *
 * <p>One step marked {@code async(true)}; nothing else changes. The thing to
 * watch is where the waiting happens — the join is the line that READS the key.
 */
package dev.devoxx.dashboard.demos._20_async;
