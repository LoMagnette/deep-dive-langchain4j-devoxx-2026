/**
 * <b>Peer-to-peer</b>
 *
 * <p>Two peers with opposed positions and NO authority over each other, which is the one situation
 * where peer-to-peer beats a supervisor: nobody can be told to give way, so the only way out is
 * a rule both will actually keep. Every household has had this argument.
 */
package dev.devoxx.dashboard.demos._11_p2p;
