/**
 * Planners written by hand, implementing LangChain4j's {@code Planner} interface.
 *
 * <p>Its own package because a custom planner is the <i>subject</i> of the talk's "middle
 * ground" section rather than plumbing for it, and it has to be readable on a projector. The
 * zoo's planners come from the library; anything in here is ours.
 */
package dev.devoxx.dashboard.planner;
