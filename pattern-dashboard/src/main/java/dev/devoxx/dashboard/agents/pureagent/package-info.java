/**
 * Agents for the supervisor — the right of the dial, where the model decides the path.
 *
 * <p>Both are named literally in the canned plan inside {@link
 * dev.devoxx.dashboard.model.MockChatModel}, so renaming either one breaks the offline
 * supervisor demo.
 */
package dev.devoxx.dashboard.agents.pureagent;
