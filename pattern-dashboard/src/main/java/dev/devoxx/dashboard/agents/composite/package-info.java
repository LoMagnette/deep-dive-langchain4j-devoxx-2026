/**
 * Agents that exist only inside the two composite systems, and are mostly <b>glue</b>: each one
 * hands one pattern's output to the next in the shape that one expects.
 *
 * <p>That is not incidental — it is where most of the work in a composite actually goes, and the
 * reason this package is as big as it is.
 */
package dev.devoxx.dashboard.agents.composite;
