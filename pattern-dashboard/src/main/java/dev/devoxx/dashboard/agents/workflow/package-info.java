/**
 * Agents for the six deterministic workflow patterns — the left of the dial, where you decide
 * the path: the sitter card and its fridge checklist, the note critic with its four rules, the
 * two walk checks that can each veto, the picnic-blanket food check, and the worry router with
 * the three people it can reach.
 */
package dev.devoxx.dashboard.agents.workflow;
